---
import "~/styles/components/post-tags/Tabs.scss";

const { tab, id } = Astro.props;
if(!id) Astro.logger.error("post-tags Tabs: No id specified.");
---
<div class="tabs" data-tab-default={tab ? tab : 1} id={id}>
    <slot />
</div>

<script src="~/scripts/Tabs.js"></script>