---
import "~/styles/components/CommonBlock.scss";

const { title, subtitle } = Astro.props;
---
<div class="common-block">
    {title && <h1>{title}</h1>}
    {subtitle && <span class="subtitle">{subtitle}</span>}
    <slot />
</div>
