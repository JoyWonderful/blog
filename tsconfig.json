{
    "compilerOptions": {
        "strict": true,
        // "types": ["node"], // install @types/node then
        // "typeRoots": ["node_modules/@types"],
        "types": ["astro/types/content"],
        "typeRoots": ["node_modules"],
        "paths": {
            "~/*": ["./src/*"]
        },
        "moduleResolution": "bundler"
    }
}